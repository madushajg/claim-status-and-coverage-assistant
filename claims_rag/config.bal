// Embedding provider configuration for the RAG knowledge base.
configurable string embeddingServiceUrl = ?;
configurable string embeddingAccessToken = ?;

// Directory containing the local Markdown policy knowledge documents.
configurable string ragDocumentsPath = "resources/rag_documents";
